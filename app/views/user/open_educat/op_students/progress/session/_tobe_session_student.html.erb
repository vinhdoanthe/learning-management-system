<%= render 'user/open_educat/shared/session_calendar_box', {:session => session} %>
<td>
  <div class="photo-box">
    <span>
      <%= thumbnail_tag  lesson%>
    </span>
  </div>
</td>
<td>
  <strong style="text-transform: uppercase; color: #6c7178 "><%= lesson.nil? ? 'Chưa diễn ra' : Learning::Course::OpLessonDecorator.new(lesson).display_lesson_with_subject %></strong><br/><span style="text-transform: uppercase; color: #6D7278">
  <%= lesson.nil? ? 'Chưa diễn ra' : lesson.name %></span> <br/>
  <%= lesson.nil? ? 'Chưa diễn ra' : sanitize(truncate(lesson.note, :length => 50)) %>
</td>
<td align="right">
</td>
