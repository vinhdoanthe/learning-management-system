<%= form_with(model: adm_redeem_redeem_product_size, local: true) do |form| %>
<% if adm_redeem_redeem_product_size.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(adm_redeem_redeem_product_size.errors.count, "error") %> prohibited this adm_redeem_redeem_product_size from being saved:</h2>

  <ul>
    <% adm_redeem_redeem_product_size.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="actions">
  <%= form.submit %>
</div>
<% end %>