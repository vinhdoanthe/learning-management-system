res = JSON.parse('<%= raw(result.to_json) %>');
display_response_noti(res);

<% if c_project&.present? %>
    window.location.href = '/contest/contest_projects/show/<%= c_project.id %>';
  <% else %>
    window.reload();
  <% end %>

